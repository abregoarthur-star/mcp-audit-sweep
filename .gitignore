node_modules/
*.log
.DS_Store
~$*

# Vendor source clones — pulled fresh via scripts/fetch-sources.sh
servers/
cf-mcp/
sb-mcp/

# Full finding details held until responsible disclosure closes
REPORT-full.md
REPORT-full.docx
reports/audits/
reports/diffs/
reports/diff-summary.json
disclosures/
# baselines/ IS tracked — snapshot of audit surfaces at a point in time,
# so future sweeps can diff against them and catch silent changes
# (new tools, widened permissions, description edits = "rug pulls").
